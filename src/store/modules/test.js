import uniq from 'uniq'
import types from '../types'

const initialState = []

const getters = {
}

const actions = {}

const mutations = {}

export default {
  state: initialState,
  getters,
  actions,
  mutations,
}
